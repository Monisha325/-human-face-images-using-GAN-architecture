# -human-face-images-using-GAN-architecture
This project focuses on generating realistic human face images using a deep learning model called Generative Adversarial Network (GAN). The model is trained on a dataset of human face images (CelebA), where it learns the underlying patterns and features of faces such as eyes, nose, skin texture, and structure.
